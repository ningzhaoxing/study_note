1. 进程
> **操作系统资源分配的基本单位**，拥有自己独立的内存空间和系统资源。一个进程可以拥有多个线程。

2. 线程
> 进程内的执行单元，**是 cpu 调度的基本单位**。 同一进程内的线程共享进程内的内存空间和资源，同时拥有自己的栈和寄存器。
> 线程通过共享内存进行通信，具有并发安全问题。

3. 协程
> **用户态的轻量级“线程”**。由用户态控制，不依赖操作系统。协程在线程中运行，共享线程中的资源。在切换协程时无需操作系统内核接入。