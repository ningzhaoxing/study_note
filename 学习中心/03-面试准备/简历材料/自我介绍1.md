面试官您好，我是宁赵星，26 届计算机专业毕业生，主要使用 Go 做后端开发，不过也熟悉 js、python、java 语言基本使用。

熟悉 Go 的主流框架，包括 gin、gorm、hertz、grpc 等，以及 mysql、redis、mq、etcd 等中间件的使用和基本原理。

在校期间我做过两个上线项目：  
一个是 **‘灵码云’**，是一个基于微服务架构的在线代码运行平台，服务间通过 gRPC 通信，用 etcd 做注册发现，并通过 WebSocket 管理代码运行容器的长连接，实现了安全的内网穿透和云资源复用。现在仍在为我们小组的博客平台提供在线运行能力。

另一个是 **‘校园成长平台’**，基于 gin 和 gorm 开发，我主要负责帖子、评论点赞和基于 SSE 的实时消息推送模块，结合 Redis 缓存和 MySQL 持久化实现高并发下的用户点赞。

此外，我在 **字节跳动飞书集成平台** 实习期间，负责触发器模块的模型设计演进与开发，解决了外部事件只返回 ID、导致消费模型与实体割裂的问题，通过新增 DataEnricher 模型实现领域层解耦。这段经历让我在领域建模、模型演进和设计取舍上都有了更深入的理解。

简历里还有一些细节，如果您感兴趣，我们可以进一步聊。