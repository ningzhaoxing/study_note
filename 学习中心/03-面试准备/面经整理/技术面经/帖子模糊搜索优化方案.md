普通方案：
```mysql
select * from message_1 where content like "%今天不开心%";
```

# 优化方案
## 1. mysql全文索引
```mysql
alter table article add fulltext index idx_full_content(content);
```

```mysql
select * from articles where match(content) against ('端口故障' in nature language mode/boolean mode);
```
