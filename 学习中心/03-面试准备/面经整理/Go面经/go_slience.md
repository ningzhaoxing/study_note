切片是一个指向数组的动态窗口，len代表切片的长度，cap代表底层数组的容量。

1. 切片的扩容策略：
    
    1. 1.18版本之前，长度小于1024为2倍扩容，大于1024为1.5倍扩容
        
    2. 1.18版本之后，长度小于256为2倍扩容，大于256根据增长因子进行扩容(增长因子会随着容量的变大减小，无限趋近于1.5)

2. 切片扩容的触发：在调用`append()`函数时，会根据新添加的元素数量，判断当前底层数组的容量是否足够容纳。如果无法容纳则创建一个新的底层数组，将原数组copy到新数组中。

