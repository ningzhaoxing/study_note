协程并不是创建的越多越好的，虽然协程对于线程来说是十分轻量级的，但是其创建和销毁也需要消耗cpu性能。

因此，面对现在以及未来网络的普遍，网民用户只会越来越多，Go的优势也会越来越大。在高并发场景下，就可以利用编程语言的层面进行优化，例如go高并发的优势。

一个是协程对于线程更加轻量级，一个线程需要占用的内存可能要到1MB，而一个协程只需要2KB。
可以像gmp调度模型，线程复用的思想。我们可以创建一个协程池，当消息队列中有消息需要消费时，可以直接从协程池中取出协程来处理，用完后再放回协程池。避免，协程的频繁创建与销毁。