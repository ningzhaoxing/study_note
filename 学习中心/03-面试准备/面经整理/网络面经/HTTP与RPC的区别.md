都是基于TCP定义了不同消息格式的应用层协议。

HTTP叫做**超文本传输协议**。
RPC叫做**远程过程调用**。

# 区别
## 服务发现
HTTP基于 **DNS服务** 解析域名获取IP地址，默认端口号80。
RPC基于专门的 **中间服务** 去保存服务名和IP信息，比如etcd等。

## 传输的内容
RPC采用体积更小的Protobuf或其他序列化协议八保存结构体数据，专门做定制化的远程过程调用，而HTTP要考虑在浏览器中各种行为。去除了许多冗余的操作，在做远程过程调用时更加合适和精简，而且性能也会更好。

