# 题目

给定一个字符串 `s` ，请你找出其中不含有重复字符的 **最长 子串** 的长度。

**示例 1:**

**输入:** s = "abcabcbb"
**输出:** 3 
**解释:** 因为无重复字符的最长子串是 `"abc"`，所以其长度为 3。

# 思路

通过两个指针来维护滑动窗口，当当前字符`s[i]`存在于滑动窗口时，则将滑动窗口的左端移动到重复字符的下一个字符`set[s[i]]+1`的下标，每次更新最大值。

算法思想：

通过两个指针实现滑动窗口，来维护窗口内部不存在重复的字符。
通过set来判断重复，同时还要判断重复的字符是否在窗口内部。

